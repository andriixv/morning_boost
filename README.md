**Текст презентації веб-додатка “Morning Boost”**

---

### **Загальний опис додатка**

Веб-додаток **“Morning Boost”** — це мінімалістичний і функціональний інструмент для створення позитивного настрою на початок дня. Його мета — допомогти користувачам зосередитися на власних цілях, почутті вдячності та внутрішньому стані, що сприяє підвищенню продуктивності й емоційного балансу.

Інтерфейс додатка побудовано у стилі **мінімалізму**: світлі тони, багато простору, плавні переходи та інтуїтивно зрозуміла навігація. Усі дані користувача зберігаються **в localStorage**, що дозволяє користувачеві повертатися до своїх записів навіть після перезавантаження сторінки.

Сайт є **односторінковим (Single Page Application)** і повністю реалізований за допомогою **HTML, CSS та JavaScript**, без використання серверної частини. Це робить його легким, швидким та адаптивним до будь-яких пристроїв — від комп’ютера до смартфона.

---

### **Детальний опис функціоналу**

#### **1. Розділ “Мій ранок”**

У цьому блоці користувача вітає повідомлення:

> “Доброго ранку, [Ім’я]!”

а також автоматично відображається **поточна дата**, сформована засобами JavaScript (`new Date().toLocaleDateString()`).
Ця функція створює відчуття персоналізації та налаштовує користувача на новий день.

---

#### **2. “Цитата дня”**

У розділі подається надихаюча цитата, яка покликана мотивувати на позитивний початок дня.
Цей елемент створений у вигляді простого **текстового контейнера**, який може динамічно змінювати вміст через JavaScript. У базовій версії цитата фіксована, але структура дозволяє легко додати масив цитат і функцію випадкового вибору для щоденного оновлення.

---

#### **3. “Три головні цілі на сьогодні”**

У цьому розділі користувач може ввести **три основні завдання дня** у текстові поля.
Після натискання кнопки “Зберегти”, усі введені дані зберігаються у **localStorage** за допомогою функції:

```js
localStorage.setItem('goals', JSON.stringify(goals));
```

Це забезпечує збереження цілей навіть після закриття вкладки чи перезавантаження сторінки.

---

#### **4. “Ранкова вдячність”**

Користувач має можливість записати, **за що він вдячний сьогодні**. Це допомагає розвивати позитивне мислення та емоційну стійкість.
Дані також зберігаються у **localStorage**, що дозволяє переглядати або змінювати їх пізніше. Поле реалізовано через елемент `<textarea>`, а функція збереження аналогічна до розділу цілей.

---

#### **5. “Натхнення”**

У розділі розміщено **фотогалерею світанків**, отриману через динамічні посилання з сервісу **Unsplash**:

```html
<img src="https://source.unsplash.com/600x400/?sunrise" alt="Світанок">
```

Кожного разу при оновленні сторінки з’являються нові світлини, що створює ефект свіжості.
Також присутній короткий **мотиваційний текст**, який нагадує про важливість спокою й гармонії на початку дня.

---

#### **6. “Мій настрій”**

Це інтерактивний розділ, де користувач може обрати **емоцію настрою** серед п’яти варіантів (від 😞 до 😄).
Вибір зберігається у localStorage разом із поточною датою. Дані мають формат JSON-об’єкта:

```json
{
  "2025-10-17": "5",
  "2025-10-16": "3"
}
```

JavaScript обробляє ці дані та виводить **історію настроїв за останні 7 днів** у вигляді смайлів з датами:

```js
const savedMoods = JSON.parse(localStorage.getItem('moodLog') || '{}');
renderMoodHistory();
```

Це створює просту, але ефективну **візуальну статистику емоційного стану**, яка допомагає користувачу відстежувати власні емоційні тенденції.

---

### **Підсумок**

Веб-додаток **“Morning Boost”** — це приклад поєднання **естетики, простоти та функціональності**.
Він сприяє розвитку **усвідомленості, вдячності та саморефлексії**, створюючи атмосферу спокою і натхнення.
Завдяки повній реалізації на **HTML, CSS та JavaScript**, додаток працює автономно, не потребує підключення до бази даних чи серверів, і може бути легко інтегрований у будь-яке навчальне чи мотиваційне середовище.
